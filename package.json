{
  "name": "weather-log",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "description": "Commits the current temperature to git every six hours, via GitHub Actions",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "update": "tsx src/update.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
